# swagger-api-codegen

## 前提条件

- node.jsインストール済
- カレントディレクトリに読み込む対象のyaml配置済

## 利用方法
### 初回のみ
```
npm install 
```
### 実行
```bash
node main.js
ファイル名を入力してください(例：sample.yaml) > [カレントディレクトリに配置されているyamlファイル名]
```
